<nav class="top-bar">
    <a href="/" class="h1">World Map</a>

    <button class="navbar-toggler"><i class="fa fa-bars"></i></button>
    <div class="head-menu">    
        <% if (typeof user != 'undefined') {%>
            <% if (section == 'map') {%>
                <div class="user-menu">
                    <div id="search" class="search-tab">
                        <input type="search" placeholder="Event Name ..." id="search-event">
                        <div class="suggestions" id="suggestions">
                        </div>
                    </div>
                    <a class="nav-item" id="add-event" href="#">Add Event</a>
                    <a class="nav-item d-none" id="update-event" href="#">Update Event</a>
                    <a class="nav-item" href="#" id="saved-events">Saved Events</a> 
                </div>
                <% if (user.is_admin) {%>
                    <div class="admin-option">
                        <a class="nav-item" href="#" id="saved-events">Pending Events
                            <span class="badge">2</span>
                        </a>   
                        <a class="nav-item" href="#" id="saved-events">Pending Contribution
                            <span class="badge">2</span>
                        </a>  
                        <a class="nav-item" href="#" id="saved-events">Reported Contribution
                            <span class="badge">2</span>
                        </a>  
                        <a class="nav-item" href="#" id="saved-events">Messages
                            <span class="badge">2</span>
                        </a>  
                        <a class="nav-item" href="#" id="saved-events">Accounts
                            <span class="badge">2</span>
                        </a> 
                        <a class="nav-item" href="#" id="saved-events">Reported Accounts
                            <span class="badge">2</span>
                        </a>  
                        <a class="nav-item" href="#" id="saved-events">Reported Events
                            <span class="badge">2</span>
                        </a>
                    </div> 
                <%} %>     
            <%} else { %>
            <a class="nav-item" id="user-name" href="/map">
                View Map
            </a>
            <%} %>
            <div class="user-account">
            <a class="nav-item" id="user-name" href="/user_profile/<%= user.username %>/"><%= user.username %></a>   
            <a href="/logout" class="nav-item">Logout</a>  
        <%} else { %>
            <a href="/register" class="nav-item">Register</a>
            <a href="/login" class="nav-item">Login</a>  
        <%} %>
        </div>
        
    </div>
</nav>

<script>
    let toggler = document.querySelector('.navbar-toggler');
    var menu = document.querySelector('.head-menu');

    $(toggler).on("click", function(e) {
        menu.classList.toggle("open");
    });
</script>